spring.application.name=autodeploy-demo

# 启用Thymeleaf缓存（开发时设为false）
spring.thymeleaf.cache=false

# 设置日志级别以便调试
logging.level.com.autodeploy.demo=DEBUG
logging.level.org.springframework.web=DEBUG

# 设置session超时时间（可选）
server.servlet.session.timeout=30m
